name: speedlimitflutter
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  location: ^3.2.4
  another_flushbar: ^1.10.8
  shared_preferences: ^0.5.12+4
  sqflite: 1.3.2
  path_provider: any
  latlong: ^0.6.1

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

